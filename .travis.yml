sudo: true
language: node_js

node_js:
  - '6'

cache:
  yarn: true
  directories:
    - node_modules
    - bower_components

before_install:
  - npm install -g npm@latest

script: 
  - npm test
  - if [ "$TRAVIS_BRANCH" = "master" -a "$TRAVIS_PULL_REQUEST" = "false" ]; bash ./deploy.sh ; fi
